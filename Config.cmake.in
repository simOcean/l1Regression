# In Config.cmake.in

@PACKAGE_INIT@
set_and_check(PROJECT_NAME_SUBMODULE @PROJECT_NAME_SUBMODULE@)


include("@PACKAGE_PREFIX@/Targets.cmake")

if(NOT TARGET ${PROJECT_NAME_SUBMODULE}::${PROJECT_NAME_SUBMODULE})
    include("${CMAKE_CURRENT_LIST_DIR}/${PROJECT_NAME_SUBMODULE}Targets.cmake")
endif()